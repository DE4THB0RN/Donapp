import 'package:flutter/material.dart';

class Padinho {
  static const EdgeInsets pequeno = EdgeInsets.all(8.0);
  static const EdgeInsets medio = EdgeInsets.all(16.0);
  static const EdgeInsets grande = EdgeInsets.all(32.0);
}
